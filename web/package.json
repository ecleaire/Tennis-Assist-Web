{
  "name": "tennis-assist-web",
  "private": true,
  "version": "1.3.69",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/postbuild.mjs",
    "test:tiebreak": "node scripts/verify-tiebreak.mjs",
    "test:gas-schema": "node scripts/verify-gas-schema.mjs",
    "test:stability": "node scripts/stability-smoke.mjs",
    "preview": "vite preview",
    "playwright": "playwright"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^25.9.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.6"
  },
  "dependencies": {
    "jsqr": "^1.4.0"
  }
}
