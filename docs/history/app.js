const managedAccounts = {
  hyogo: { label: "WRO兵庫", url: "https://script.google.com/macros/s/AKfycbw0wWKqqar4adDt9SXKmQdO82twKvUjomcrfYGvb7_2mi1cP5rVW7QR62Ijuc5uNpJRgQ/exec", sheet: "1pxTMvdcpTMFeSfroOeTyh2hziLgfAvLxe0Nh79sMk_0" },
  mie: { label: "WRO三重", url: "https://script.google.com/macros/s/AKfycbx6OkFR799hYZ3DaYWxfluCTuDKf6sE34HtVuzMHTfJQd5Hs0YcQujZiVxtEOxzvN5-/exec", sheet: "185jPLjc-nBri49aOr-CVw1baUI1qaxqjgcWLRS2-oxo" },
  mie_judge: { label: "WRO三重", url: "https://script.google.com/macros/s/AKfycbx6OkFR799hYZ3DaYWxfluCTuDKf6sE34HtVuzMHTfJQd5Hs0YcQujZiVxtEOxzvN5-/exec", sheet: "185jPLjc-nBri49aOr-CVw1baUI1qaxqjgcWLRS2-oxo" },
  nara: { label: "WRO奈良", url: "https://script.google.com/macros/s/AKfycbya7EhTdbZzvZIPR2HKMBha7ciFLpG-iFr1T5PZitsLgsWTXE-5lNbACIN9Bkgf_ZdE4g/exec", sheet: "1qaT1lLCqUjw__0jkR51KIqLXur3jO7EBoc8R0gWgnZ8" },
  judge: { label: "WRO共有確認用", url: "https://script.google.com/macros/s/AKfycbyniW9kgzwtMI0i5X5ZtDlnqGz1yaeuHnXZZ7s67fIS54tdzg1U__sZUzLDoLqUY8lt/exec", sheet: "1BTByUtO5IAdwdTYCMNhFUtqeRy2yIWpAnCZRQw_b0HU" },
  train: { label: "審判練習", url: "https://script.google.com/macros/s/AKfycbxd1h_jzSECSjtQIxKvoX-joGUEy2yHcJYc2nQ14-YHze9OpqXrfy9JsEg_6gi03KpA/exec", sheet: "1Bh5FpSOjkTRRV9feZ90dLXl86v3UNsG896DfhSPHst0" },
  practice: { label: "審判練習", url: "https://script.google.com/macros/s/AKfycbxd1h_jzSECSjtQIxKvoX-joGUEy2yHcJYc2nQ14-YHze9OpqXrfy9JsEg_6gi03KpA/exec", sheet: "1Bh5FpSOjkTRRV9feZ90dLXl86v3UNsG896DfhSPHst0" },
  rsam: { label: "自分", url: "https://script.google.com/macros/s/AKfycbwbs-mgIJNX-DkgtoLzpkQaTQNa75tWwijAfyudWbi4LvKJGkWSrC6y0PC_EY4kFUsa/exec", sheet: "1PKAZgb8HZFww-P9CZTkzVqleAtIOFgkl8Ngk6lZwcTA" },
  gas: { label: "自分", url: "https://script.google.com/macros/s/AKfycbwbs-mgIJNX-DkgtoLzpkQaTQNa75tWwijAfyudWbi4LvKJGkWSrC6y0PC_EY4kFUsa/exec", sheet: "1PKAZgb8HZFww-P9CZTkzVqleAtIOFgkl8Ngk6lZwcTA" },
  wrorsam: { label: "自分", url: "https://script.google.com/macros/s/AKfycbwbs-mgIJNX-DkgtoLzpkQaTQNa75tWwijAfyudWbi4LvKJGkWSrC6y0PC_EY4kFUsa/exec", sheet: "1PKAZgb8HZFww-P9CZTkzVqleAtIOFgkl8Ngk6lZwcTA" },
  shukugawa: { label: "夙川", url: "https://script.google.com/macros/s/AKfycbwZjAa77dzxEWivtFkZIWGzDdhynAFBjmn3zjdte_KO1eDbhLR0xidIv1mNTvCwwLfIzQ/exec", sheet: "1tOyTdp7DD1lFZr5XsYnB3Zc4JEM9rGeMoH_6B43Yeg4" },
};
let session = null;
let rows = [];
const loginStorageKey = "tennis-assist-history-login-v1";
const historyCacheStorageKey = "tennis-assist-history-cache-v1";
const publicLinkGroups = [
  { title: "WROホームページ", links: [["WRO Japan", "https://www.wroj.org/action/2026", "WRO Japan 公式サイト"], ["WRO 国際", "https://wro-association.org/", "WRO International"]] },
  { title: "WRO公認予選会", links: [["WRO兵庫", "https://wro-hyogo.jp/", "公認予選会"]] },
  { title: "ルール関連", links: [["Japan決勝大会ルールPDF", "https://drive.google.com/file/d/1JMmggxMfSWABUcA5sbM9U3qffb-ZMb2U/view?usp=sharing", "公式ルール原文"], ["世界大会公式ルール", "https://wro-association.org/wp-content/uploads/WRO-2026-RoboSports-Double-Tennis-General-Rules.pdf", "WRO International"], ["Google翻訳ルール", "https://drive.google.com/file/d/16zFJ_bD8sfLZZF6QkRCWQ6azN_Dj3eUG/view", "翻訳版"], ["DeepL翻訳ルール", "https://drive.google.com/file/d/1z_Q7M7lP2Q55Zo3qZgzH-bN_QqhCx-wJ/view", "翻訳版"], ["公式Q&A", "https://wro-association.org/competition/questions-answers/", "ルール質問と回答"]] },
  { title: "その他", links: [["GitHubリポジトリ", "https://github.com/ecleaire/Tennis-Assist-Web", "アプリのソースコード"]] },
  { title: "公開URL QRコード", qr: true, links: [["generalを開く", "https://ecleaire.github.io/Tennis-Assist-Web/general/", "選手・練習用 general"]] },
];
const $ = (id) => document.getElementById(id);
const text = (value) => String(value ?? "").trim();
const number = (value) => Number(text(value).replace(/[^\d.-]/g, "")) || 0;
const escapeHtml = (value) => text(value).replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[char]));
const animationSettingsStorageKey = "tennis-assist-history-animation-v1";
const glitchPatterns = ["horizontal", "diagonal", "blocks", "grid", "digital", "scanline"];
const defaultAnimationSettings = { enabled: true, intervalMinutes: 3, durationSeconds: 0.76, noiseStrength: 88, noisePattern: "random", lineGap: 110 };
function clamp(value, min, max) { return Math.min(max, Math.max(min, Number(value) || min)); }
function loadAnimationSettings() { try { const saved = JSON.parse(localStorage.getItem(animationSettingsStorageKey) || "null"); return saved ? { enabled: saved.enabled !== false, intervalMinutes: clamp(saved.intervalMinutes ?? defaultAnimationSettings.intervalMinutes, 0.5, 60), durationSeconds: clamp(saved.durationSeconds ?? defaultAnimationSettings.durationSeconds, 0.2, 3), noiseStrength: clamp(saved.noiseStrength ?? defaultAnimationSettings.noiseStrength, 0, 100), noisePattern: saved.noisePattern === "random" || glitchPatterns.includes(saved.noisePattern) ? saved.noisePattern : "random", lineGap: clamp(saved.lineGap ?? defaultAnimationSettings.lineGap, 0, 500) } : { ...defaultAnimationSettings }; } catch { return { ...defaultAnimationSettings }; } }
let animationSettings = loadAnimationSettings();
let dataGlitchTimer = 0;
let periodicGlitchTimer = 0;
let rulePdfLoadTimer = 0;
let rulePdfStartTimer = 0;
let rulePdfRequestId = 0;
let currentRulePdfExternalUrl = "https://drive.google.com/file/d/1JMmggxMfSWABUcA5sbM9U3qffb-ZMb2U/view?usp=sharing";
function dataModeActive() { return Boolean(session) && !$("viewer").classList.contains("hidden"); }
function chooseGlitchPattern() { return animationSettings.noisePattern === "random" ? glitchPatterns[Math.floor(Math.random() * glitchPatterns.length)] : animationSettings.noisePattern; }
function configureGlitchText(element, index) { const strength = animationSettings.noiseStrength / 100; const width = Math.max(1, element.scrollWidth || element.getBoundingClientRect().width); const bandWidth = 50 + strength * 125 + Math.random() * 55; const duration = animationSettings.durationSeconds * 1000; const shift = 0.8 + strength * 6.2; const verticalShift = Math.random() * 2.4 - 1.2; element.classList.add("history-glitch"); element.dataset.pattern = chooseGlitchPattern(); element.style.setProperty("--delay", `${index * animationSettings.lineGap}ms`); element.style.setProperty("--dur", `${duration}ms`); element.style.setProperty("--jitter", `${Math.round(index * animationSettings.lineGap + duration * 0.56)}ms`); element.style.setProperty("--width", `${Math.round(bandWidth)}px`); element.style.setProperty("--alpha", String(0.28 + strength * 0.72)); element.style.setProperty("--gx", `${shift.toFixed(1)}px`); element.style.setProperty("--gxn", `${(-shift).toFixed(1)}px`); element.style.setProperty("--gy", `${verticalShift.toFixed(1)}px`); element.style.setProperty("--gyn", `${(-verticalShift).toFixed(1)}px`); element.style.setProperty("--start", `${Math.round(-bandWidth - 18)}px`); element.style.setProperty("--end", `${Math.round(width + bandWidth + 20)}px`); }
function playTextGlitch(target, force = false) { if (!target || (!force && !animationSettings.enabled) || !animationSettings.noiseStrength || window.matchMedia("(prefers-reduced-motion: reduce)").matches) return; window.clearTimeout(dataGlitchTimer); document.querySelectorAll(".is-glitch-revealing").forEach((element) => element.classList.remove("is-glitch-revealing")); document.querySelectorAll(".history-glitch").forEach((element) => element.classList.remove("history-glitch")); const textElements = [...target.querySelectorAll("h1, h2, h3, .eyebrow, .stat-card span, .stat-card strong, .toolbar strong, .toolbar p, .history-card p, .history-card h3, #history-status, .public-link-card strong, .public-link-card span, .public-qr-card strong, .public-qr-card small")].filter((element) => { const rect = element.getBoundingClientRect(); return !element.closest(".hidden") && element.textContent.trim() && rect.bottom >= 0 && rect.top <= window.innerHeight; }); textElements.forEach((element, index) => configureGlitchText(element, index % 6)); void target.offsetWidth; target.classList.add("is-glitch-revealing"); const totalDuration = animationSettings.durationSeconds * 1000 + Math.min(5, Math.max(0, textElements.length - 1)) * animationSettings.lineGap + 350; dataGlitchTimer = window.setTimeout(() => { target.classList.remove("is-glitch-revealing"); textElements.forEach((element) => element.classList.remove("history-glitch")); }, totalDuration); }
function schedulePeriodicGlitch() { window.clearInterval(periodicGlitchTimer); periodicGlitchTimer = 0; if (!animationSettings.enabled || !dataModeActive()) return; periodicGlitchTimer = window.setInterval(() => { if (dataModeActive() && document.visibilityState === "visible") playTextGlitch($("viewer")); }, animationSettings.intervalMinutes * 60000); }
function showPublicPane(name, animate = false) { const loggedInHome = name === "login-panel" && Boolean(session); document.querySelectorAll(".public-pane").forEach((pane) => pane.classList.toggle("hidden", loggedInHome || pane.id !== name)); $("viewer").classList.toggle("hidden", !loggedInHome); document.querySelectorAll(".public-nav button").forEach((button) => button.classList.toggle("active", button.id === `public-${name.replace("public-", "")}-tab`)); if (name === "public-rules") ensureRulePdfLoaded(); const animationTarget = loggedInHome ? $("viewer") : name === "public-links" ? $("public-links") : name === "public-rules" ? document.querySelector("#public-rules .section-heading") : null; if (animate && animationTarget) playTextGlitch(animationTarget); schedulePeriodicGlitch(); }
function setRulePdfState(state, loadingText, note) { const viewer = $("rule-pdf-viewer"); viewer.classList.remove("is-idle", "is-loading", "is-ready", "is-slow", "is-offline"); viewer.classList.add(state); $("rule-pdf-loading-text").textContent = loadingText; $("rule-pdf-note").textContent = note; }
function loadRulePdf(source, force = false) { const frame = $("rule-pdf-frame"); if (!source || (!force && frame.dataset.loadedSrc === source)) return; const requestId = ++rulePdfRequestId; window.clearTimeout(rulePdfLoadTimer); window.clearTimeout(rulePdfStartTimer); frame.dataset.loadedSrc = source; if (!navigator.onLine) { setRulePdfState("is-offline", "インターネット接続がありません", "接続が戻ると自動的に再読み込みします。"); return; } setRulePdfState("is-loading", "PDFを読み込んでいます", "PDFを読み込んでいます..."); const startLoad = () => { if (requestId !== rulePdfRequestId) return; frame.onload = () => { if (requestId !== rulePdfRequestId || frame.src === "about:blank") return; window.clearTimeout(rulePdfLoadTimer); setRulePdfState("is-ready", "PDFを読み込んでいます", "PDFを表示しています。表示できない場合は別タブで開いてください。"); }; frame.onerror = () => { if (requestId !== rulePdfRequestId) return; window.clearTimeout(rulePdfLoadTimer); setRulePdfState("is-slow", "PDFを表示できません", "再読み込みするか、別タブで開いてください。"); }; frame.src = source; rulePdfLoadTimer = window.setTimeout(() => { if (requestId !== rulePdfRequestId) return; setRulePdfState("is-slow", "PDFの接続を待っています", "読み込みに時間がかかっています。再読み込みするか、別タブで開いてください。"); }, 15000); }; if (force && frame.src !== "about:blank") { frame.src = "about:blank"; rulePdfStartTimer = window.setTimeout(startLoad, 80); } else startLoad(); }
function ensureRulePdfLoaded() { const frame = $("rule-pdf-frame"); loadRulePdf(frame.dataset.loadedSrc || frame.dataset.src); }
function renderPublicLinks() { $("public-links-list").innerHTML = publicLinkGroups.map((group) => `<section class="public-link-group"><h3>${escapeHtml(group.title)}</h3>${group.qr ? `<a class="public-qr-card" href="${group.links[0][1]}" target="_blank" rel="noopener"><img src="../assets/qr-general.png" alt="general公開URL QRコード" loading="lazy"><span><strong>${escapeHtml(group.links[0][0])}</strong><small>${escapeHtml(group.links[0][2])}</small><small>${escapeHtml(group.links[0][1])}</small></span></a>` : `<div class="public-link-grid">${group.links.map(([label, url, description]) => `<a class="public-link-card" href="${url}" target="_blank" rel="noopener"><strong>${escapeHtml(label)}</strong><span>${escapeHtml(description)}</span><span>${escapeHtml(url)}</span></a>`).join("")}</div>`}</section>`).join(""); }
function rowValue(row, name) { return row[session.columns.indexOf(name)] ?? ""; }
function normalizedPassword(value) { return text(value).toLowerCase().replace(/\s+/g, ""); }
function dateValue(value) { const parsed = new Date(text(value).replace(" ", "T")); return Number.isNaN(parsed.getTime()) ? 0 : parsed.getTime(); }
function dateBounds() { const value = $("period").value; const now = new Date(); const today = new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime(); let from = 0; let to = Number.POSITIVE_INFINITY; if (value === "today") { from = today; to = today + 86400000 - 1; } else if (value === "yesterday") { from = today - 86400000; to = today - 1; } else if (value === "day-before-yesterday") { from = today - 2 * 86400000; to = today - 86400000 - 1; } else if (value === "week" || value === "month") { from = Date.now() - (value === "week" ? 7 : 31) * 86400000; } if ($("date-from").value) from = Math.max(from, new Date(`${$("date-from").value}T00:00:00`).getTime()); if ($("date-to").value) to = Math.min(to, new Date(`${$("date-to").value}T23:59:59.999`).getTime()); return { from, to }; }
function isUnsent(row) { const status = rowValue(row, "送信状態") || rowValue(row, "sendStatus"); return status === "pending" || status === "failed" || rowValue(row, "未送信") === "TRUE"; }
function historyCacheKey(account) { return `${historyCacheStorageKey}:${account.sheet}`; }
function restoreHistoryCache(account) { try { const cached = JSON.parse(localStorage.getItem(historyCacheKey(account)) || "null"); if (!cached || !Array.isArray(cached.columns) || !Array.isArray(cached.rows) || Date.now() - Number(cached.savedAt) > 7 * 86400000) return false; session.columns = cached.columns; rows = cached.rows; render(); $("loaded-at").textContent = `保存データを表示中（${new Date(cached.savedAt).toLocaleString("ja-JP")}）`; return true; } catch { return false; } }
function saveHistoryCache() { if (!session || !$("remember-login").checked) return; try { localStorage.setItem(historyCacheKey(session.account), JSON.stringify({ columns: session.columns, rows, savedAt: Date.now() })); } catch { /* 容量不足時は通常表示を継続 */ } }
function showLoggedInView(account, animate = true) { $("account-label").textContent = account.label; $("account-label").classList.remove("hidden"); $("logout").classList.remove("hidden"); $("login-status").textContent = ""; showPublicPane("login-panel"); if (animate) playTextGlitch($("viewer")); }
async function loadHistory() {
  const params = new URLSearchParams({ action: "history", api_key: session.apiKey, spreadsheet_id: session.account.sheet, sheet: "対戦履歴archive" });
  const response = await fetch(`${session.account.url}?${params}`);
  const data = await response.json();
  if (!response.ok || data.ok === false) throw new Error(data.error || "履歴の読み込みに失敗しました");
  session.columns = data.csv_columns || [];
  rows = (data.csv_rows || []).filter((row) => rowValue(row, "記録種別") === "試合結果" || rowValue(row, "記録種別") === "マッチ");
  render();
  $("loaded-at").textContent = `更新 ${new Date().toLocaleString("ja-JP")}`;
  saveHistoryCache();
}
function render() {
  const bounds = dateBounds();
  const filtered = rows.filter((row) => { const stamp = dateValue(rowValue(row, "日時")); return stamp >= bounds.from && stamp <= bounds.to; });
  const series = filtered.filter((row) => rowValue(row, "記録種別") === "試合結果");
  const matches = filtered.filter((row) => rowValue(row, "記録種別") === "マッチ");
  const allSeries = rows.filter((row) => rowValue(row, "記録種別") === "試合結果");
  const allMatches = rows.filter((row) => rowValue(row, "記録種別") === "マッチ");
  const allTeams = [...new Set(rows.flatMap((row) => [rowValue(row, "チームA"), rowValue(row, "チームB")]).filter(Boolean))].sort((a, b) => a.localeCompare(b, "ja"));
  $("stat-series").textContent = allSeries.length; $("stat-matches").textContent = allMatches.length; $("stat-teams").textContent = allTeams.length;
  $("stat-latest").textContent = rows.length ? text(rows.map((row) => rowValue(row, "日時")).sort().at(-1)).slice(0, 10) : "--";
  const selected = $("team-filter").value;
  const statsTeam = $("stats-team").value;
  const optionsKey = allTeams.join("\u0000");
  const optionsMarkup = `<option value="">すべてのチーム</option>${allTeams.map((team) => `<option value="${escapeHtml(team)}">${escapeHtml(team)}</option>`).join("")}`;
  if ($("team-filter").dataset.options !== optionsKey) { $("team-filter").innerHTML = optionsMarkup; $("team-filter").value = selected; $("team-filter").dataset.options = optionsKey; }
  if ($("stats-team").dataset.options !== optionsKey) { $("stats-team").innerHTML = `<option value="">チームを選択</option>${allTeams.map((team) => `<option value="${escapeHtml(team)}">${escapeHtml(team)}</option>`).join("")}`; $("stats-team").value = statsTeam; $("stats-team").dataset.options = optionsKey; }
  renderStats(matches, statsTeam ? [statsTeam] : []); renderHistory(filtered);
}
function renderStats(matches, teams) {
  const metrics = $("selected-team-stats");
  const rows = $("stats-team").value ? teams.map((team) => {
    const related = matches.filter((row) => rowValue(row, "チームA") === team || rowValue(row, "チームB") === team);
    const wins = related.filter((row) => rowValue(row, "マッチ勝者") === team || rowValue(row, "総合勝者") === team).length;
    const draws = related.filter((row) => rowValue(row, "マッチ勝者") === "引き分け" || rowValue(row, "総合勝者") === "引き分け").length;
    const losses = Math.max(0, related.length - wins - draws);
    const purple = related.reduce((sum, row) => sum + (rowValue(row, "チームA") === team ? number(rowValue(row, "チームA紫")) : number(rowValue(row, "チームB紫"))), 0);
    const totalPurple = related.reduce((sum, row) => sum + number(rowValue(row, "チームA紫")) + number(rowValue(row, "チームB紫")), 0);
    const purpleRate = totalPurple ? `${(purple / totalPurple * 100).toFixed(1)}%` : "0.0%";
    const violations = related.reduce((sum, row) => sum + (rowValue(row, "チームA") === team ? number(rowValue(row, "チームA違反数")) : number(rowValue(row, "チームB違反数"))), 0);
    const rate = related.length ? `${(wins / related.length * 100).toFixed(1)}%` : "0.0%";
    return { team, related, wins, draws, losses, purpleRate, violations, rate };
  }) : [];
  const selected = $("stats-team").value ? rows[0] : null;
  metrics.innerHTML = selected ? [["マッチ数", selected.related.length], ["勝敗", `${selected.wins}勝 ${selected.losses}敗 ${selected.draws}分`], ["勝率", selected.rate], ["紫取得率", selected.purpleRate], ["違反数", selected.violations]].map(([label, value]) => `<article class="stat-card"><span>${label}</span><strong>${value}</strong></article>`).join("") : "";
}
function renderHistory(filtered) {
  const team = $("team-filter").value; const result = $("result-filter").value; const kind = $("kind-filter").value;
  const visible = filtered.filter((row) => { if (team && rowValue(row, "チームA") !== team && rowValue(row, "チームB") !== team) return false; const recordKind = rowValue(row, "記録種別"); if (kind === "match" && recordKind !== "マッチ") return false; if (kind === "series" && recordKind !== "試合結果") return false; if (kind === "unsent" && !isUnsent(row)) return false; if (result !== "all") { const winner = recordKind === "試合結果" ? rowValue(row, "総合勝者") : rowValue(row, "マッチ勝者"); if (result === "draw" && winner !== "引き分け") return false; if (team && result === "win" && winner !== team) return false; if (team && result === "loss" && (winner === team || winner === "引き分け")) return false; } return true; });
  const ordered = [...visible].sort((a, b) => dateValue(rowValue(b, "日時")) - dateValue(rowValue(a, "日時"))); if ($("sort-filter").value === "old") ordered.reverse(); const latest = ordered.slice(0, 100); const list = $("history-list");
  $("history-status").textContent = `保存済み ${rows.length}件 / 確認用 0件 / 表示 ${visible.length}件`;
  list.innerHTML = latest.map((row) => { const a = rowValue(row, "チームA"); const b = rowValue(row, "チームB"); const winner = rowValue(row, "記録種別") === "試合結果" ? rowValue(row, "総合勝者") : rowValue(row, "マッチ勝者"); return `<article class="history-card"><h3>${escapeHtml(a)} <span class="muted">vs</span> ${escapeHtml(b)}</h3><p>${escapeHtml(rowValue(row, "日時"))} / ${escapeHtml(rowValue(row, "コート"))} / ${escapeHtml(rowValue(row, "種別"))} / 第${escapeHtml(rowValue(row, "マッチ番号") || "-")}マッチ</p><p class="winner">勝者: ${escapeHtml(winner || "未確定")}　得点 ${escapeHtml(rowValue(row, "チームA得点"))} - ${escapeHtml(rowValue(row, "チームB得点"))}　紫 ${escapeHtml(rowValue(row, "チームA紫"))} - ${escapeHtml(rowValue(row, "チームB紫"))}</p></article>`; }).join("") || `<p class="muted">履歴がありません。</p>`;
}
function animationSettingsFromForm() { return { enabled: $("glitch-enabled").checked, intervalMinutes: clamp($("glitch-interval-minutes").value, 0.5, 60), durationSeconds: clamp($("glitch-duration-seconds").value, 0.2, 3), noiseStrength: clamp($("glitch-noise-strength").value, 0, 100), noisePattern: $("glitch-noise-pattern").value, lineGap: clamp($("glitch-line-gap").value, 0, 500) }; }
function setAnimationControlsDisabled(disabled) { ["glitch-interval-minutes", "glitch-duration-seconds", "glitch-noise-strength", "glitch-noise-pattern", "glitch-line-gap"].forEach((id) => { $(id).disabled = disabled; }); }
function syncAnimationSettingsForm() { $("glitch-enabled").checked = animationSettings.enabled; $("glitch-interval-minutes").value = animationSettings.intervalMinutes; $("glitch-duration-seconds").value = animationSettings.durationSeconds; $("glitch-noise-strength").value = animationSettings.noiseStrength; $("glitch-noise-pattern").value = animationSettings.noisePattern; $("glitch-line-gap").value = animationSettings.lineGap; setAnimationControlsDisabled(!animationSettings.enabled); $("animation-settings-status").textContent = ""; }
function openAnimationSettings() { syncAnimationSettingsForm(); $("animation-settings-dialog").showModal(); }
function saveAnimationSettings(event) { event.preventDefault(); animationSettings = animationSettingsFromForm(); localStorage.setItem(animationSettingsStorageKey, JSON.stringify(animationSettings)); schedulePeriodicGlitch(); $("animation-settings-status").textContent = animationSettings.enabled ? `${animationSettings.intervalMinutes}分間隔で保存しました。` : "グリッチ演出を無効にしました。"; window.setTimeout(() => $("animation-settings-dialog").close(), 450); }
async function login(event) { event.preventDefault(); const password = normalizedPassword($("password").value); const account = managedAccounts[password]; if (!account) { $("login-status").textContent = "管理者パスワードを確認してください。"; return; } let apiKey = text($("api-key").value); if (!apiKey) { try { const saved = JSON.parse(localStorage.getItem("tennis-assist-admin-v1") || "{}"); apiKey = text(saved.apiKey); } catch { apiKey = ""; } } if (!apiKey) apiKey = password === "shukugawa" ? "GAS" : password; if ($("remember-login").checked) localStorage.setItem(loginStorageKey, JSON.stringify({ remember: true, password, apiKey })); else localStorage.removeItem(loginStorageKey); session = { account, apiKey, columns: [] }; const restored = $("remember-login").checked && restoreHistoryCache(account); if (restored) showLoggedInView(account, false); else $("login-status").textContent = "履歴を読み込んでいます..."; try { await loadHistory(); if (restored) playTextGlitch($("viewer")); else showLoggedInView(account); } catch (error) { if (restored) { $("history-status").textContent = `保存データを表示中：${error.message}`; return; } session = null; $("login-status").textContent = `${error.message}。APIキーを入力して再試行してください。`; } }
function logout() { session = null; rows = []; $("account-label").classList.add("hidden"); $("logout").classList.add("hidden"); showPublicPane("login-panel"); }
$("login-form").addEventListener("submit", login); $("login-form").addEventListener("keydown", (event) => { if (event.key !== "Enter" || event.isComposing) return; event.preventDefault(); $("login-form").requestSubmit(); }); $("logout").addEventListener("click", logout); $("refresh").addEventListener("click", async () => { playTextGlitch($("viewer")); try { $("history-status").textContent = "再読み込み中..."; await loadHistory(); } catch (error) { $("history-status").textContent = error.message; } }); ["period", "date-from", "date-to", "stats-team", "team-filter", "result-filter", "kind-filter", "sort-filter"].forEach((id) => $(id).addEventListener("change", () => { render(); if (id === "kind-filter") playTextGlitch($("viewer")); }));
$("public-login-tab").addEventListener("click", () => showPublicPane("login-panel", true)); $("public-links-tab").addEventListener("click", () => showPublicPane("public-links")); $("public-rules-tab").addEventListener("click", () => showPublicPane("public-rules")); $("animation-settings-open").addEventListener("click", openAnimationSettings); $("animation-settings-close").addEventListener("click", () => $("animation-settings-dialog").close()); $("animation-settings-form").addEventListener("submit", saveAnimationSettings); $("glitch-enabled").addEventListener("change", () => setAnimationControlsDisabled(!$("glitch-enabled").checked)); $("glitch-preview").addEventListener("click", () => { const savedSettings = animationSettings; animationSettings = { ...animationSettingsFromForm(), enabled: true }; playTextGlitch($("animation-settings-dialog"), true); animationSettings = savedSettings; }); document.querySelectorAll("[data-rule-pdf-src]").forEach((link) => link.addEventListener("click", (event) => { const source = link.dataset.rulePdfSrc; if (!source) return; event.preventDefault(); currentRulePdfExternalUrl = link.href; loadRulePdf(source); })); $("rule-pdf-reload").addEventListener("click", () => loadRulePdf($("rule-pdf-frame").dataset.loadedSrc || $("rule-pdf-frame").dataset.src, true)); $("rule-pdf-open").addEventListener("click", () => window.open(currentRulePdfExternalUrl, "_blank", "noopener")); $("rule-pdf-fullscreen").addEventListener("click", async () => { try { await $("rule-pdf-viewer").requestFullscreen(); } catch { window.open(currentRulePdfExternalUrl, "_blank", "noopener"); } }); $("rule-search-button").addEventListener("click", () => { const query = $("rule-search").value.trim(); if (query) { currentRulePdfExternalUrl = "https://drive.google.com/file/d/1JMmggxMfSWABUcA5sbM9U3qffb-ZMb2U/view?usp=sharing"; loadRulePdf(`https://drive.google.com/file/d/1JMmggxMfSWABUcA5sbM9U3qffb-ZMb2U/preview#search=${encodeURIComponent(query)}`, true); } }); window.addEventListener("offline", () => { if (!$("rule-pdf-viewer").classList.contains("is-loading")) return; ++rulePdfRequestId; window.clearTimeout(rulePdfLoadTimer); window.clearTimeout(rulePdfStartTimer); setRulePdfState("is-offline", "インターネット接続がありません", "接続が戻ると自動的に再読み込みします。"); }); window.addEventListener("online", () => { if (!$("public-rules").classList.contains("hidden") && $("rule-pdf-viewer").classList.contains("is-offline")) loadRulePdf($("rule-pdf-frame").dataset.loadedSrc || $("rule-pdf-frame").dataset.src, true); }); renderPublicLinks();
$("public-links-tab").addEventListener("click", () => playTextGlitch($("public-links")));
$("public-rules-tab").addEventListener("click", () => playTextGlitch(document.querySelector("#public-rules .section-heading")));
try { const savedLogin = JSON.parse(localStorage.getItem(loginStorageKey) || "null"); if (savedLogin && savedLogin.remember === true) { $("password").value = text(savedLogin.password); $("api-key").value = text(savedLogin.apiKey); $("remember-login").checked = true; login({ preventDefault() {} }); } } catch { localStorage.removeItem(loginStorageKey); }
